# Current

- Branch: `main`, local only.
- Task: Slice 5 UI simplification, Standard Mode. Plan: `kis/intent/slice-5-ui.md`.
- Command: `cd frontend && npm run typecheck && npm run build` / `cd backend && uv run pytest -m eval`.
- Blocker: none.
- Next: build Home, Today, Attention, All, shell; then headless proof; then eval.
