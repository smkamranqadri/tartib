# Current

- Branch: `main`, local only.
- Task: Slice 4 classifier fixes, Phase Mode. Plan: `kis/intent/slice-4-classifier.md`. Now in Phase 1 (backend).
- Command: `cd backend && uv run pytest -q`.
- Blocker: none.
- Next: Phase 1 backend, then frontend, then eval + docs + live migration (copy the volume DB first).
