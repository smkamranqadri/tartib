# Current

- Branch: `main`, local only.
- Task: none. Slice 8 feedback round complete and deployed 2026-09-17.
- Command: `cd backend && uv run pytest -q` / `cd frontend && npm run build` / `docker compose up -d --build`.
- Blocker: none. Claude fallback inside Docker still needs `CLAUDE_CODE_OAUTH_TOKEN`.
- Next: user decides. Live container on http://localhost:8000. Backups in the session scratchpad `backup/` (latest: `tartib-pre-slice8.db`).

## Proof (2026-09-17, slice 8)

- `uv run pytest -q`: 92 passed (spaces seed once, create validation, rename cascade incl. brief, delete only when empty, classifier prompt lists a created space, item text edit indexed by search, delete keeps capture, brief unchanged by a done tick and regenerated by a new item, recent page, migration 0004).
- `npm run typecheck`, `npm run build`: clean.
- Headless Chrome, phone 390 and desktop 1280, seeded fake classifier: phone card order Today, Needs attention, Recent; Recent shows 3 and View all lists 8 on /recent; capture box grows 46 -> 94px over three Shift+Enter lines and Enter files the multi-line note; Inbox Waiting lists all 3, tapping the third makes it the card and Enter approves it; Inbox Recent shows 3; nav Home · Inbox · Spaces · Settings; New space "gym" appears in chips and the classifier files "bench press" into it; brief: done tick 0 Codex calls, new capture regenerates; delete disabled while the space has items, rename gym -> fitness moves the chip; note title opens the item page; both accordions closed and empty on a filed item, open on a waiting one; Edit text saves and search finds it; delete removes the item, keeps the capture, navigates back; empty space delete works. No page errors.
- Live: backup `tartib-pre-slice8.db`, migration 0004 on the volume, spaces table seeded with the 7 existing spaces, health ok.

## Known gaps

- Real dictation still depends on the browser.
- Brief only refreshes on add/remove or the refresh icon, by design; a done tick leaves the old text until then.
